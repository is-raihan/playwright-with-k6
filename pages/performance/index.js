/**
 * Performance Page Objects Index
 * Exports all performance page objects for easy importing
 */
const { BasePerformance } = require('./base.perf');
const { AuthPerformance } = require('./auth.perf');

module.exports = {
  BasePerformance,
  AuthPerformance
};